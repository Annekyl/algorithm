#include "bits/stdc++.h"
#define int long long
#define endl '\n'
using namespace std;

const int mod = 1e9 + 7;

void solve() {
    int n, q;
    cin >> n >> q;
    while(q--){
        int l,r;
        cin>>l>>r;
    }
}

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t = 1;
    // cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}