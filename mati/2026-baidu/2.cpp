#include "bits/stdc++.h"
#define int long long

using namespace std;

typedef vector<int> vi;

void solve() {
    int n, m, k;
    cin >> n >> m >> k;
    vi a(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t = 1;
    cin >> t;
    while (t--)
        solve();

    return 0;
}